@page "/"
@inject HttpClient _http

@ArmorJson;

@code
{
    public string? ArmorJson = string.Empty;

    protected override async Task OnInitializedAsync()
    {
        ArmorJson = await _http.GetStringAsync("/elden-ring-armor-optimizer/data/armor.json");
    }
}
